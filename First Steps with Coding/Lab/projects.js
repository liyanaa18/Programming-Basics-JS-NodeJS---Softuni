function architectProjects(input)
{
    let name = input[0];
    let project = input[1];
    let hours = 3 * input[1];

    console.log(`The architect ${name} will need ${hours} hours to complete ${project} project/s.`)
}

architectProjects(["George ",

"4 "]);